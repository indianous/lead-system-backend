package br.com.joaovictornascimento.lead_system.domain;

/** Reaproveitado em {@link Lead#getLeadType()} e {@link LeadOrigin#getOriginType()} — mesmo significado. */
public enum LeadType {
	DIRECT_CONTACT, LOCAL_SEARCH
}
